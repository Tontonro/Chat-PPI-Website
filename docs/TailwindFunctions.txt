container → limita largura
mx-auto → centraliza horizontal
px-4 px-2 → padding(O padding é o espaço entre o conteúdo e a borda da caixa) horizontal
py-1 py-2 py-4 py-8 → padding vertical
p-4 → padding geral
mt-6 mt-8 → margem topo
mb-1 mb-3 mb-4 → margem baixo

flex → ativa flexbox(é um modelo de layout do CSS usado pra organizar elementos de forma flexível e responsiva dentro de um container)
justify-center → centraliza horizontal
gap-6 → espaço entre itens

w-full → largura 100%
max-w-sm → largura máxima pequena

bg-black → fundo preto
text-[#00ff00] → texto verde
text-[#008800] → texto verde escuro
border-[#00ff00] → borda verde
placeholder-[#008800] → cor placeholder

border → borda
border-b → borda embaixo
border-t → borda em cima

text-xs text-sm text-lg → tamanho texto
font-bold → negrito
font-mono → fonte mono
antialiased → suaviza fonte

hover:text-white → muda texto no hover
hover:bg-[#001100] → muda fundo no hover
focus:outline-none → remove outline
focus:bg-[#001100] → muda fundo no foco

hidden → escondido
block → display block
list-none → remove estilo lista
m-0 → margem 0
p-0 → padding 0
transition → animação
text-center → centraliza texto